# API & Data Essentials — Part 3 Plan

## Goal
SQL basics for testers (SELECT/JOIN), seed/teardown, test-data strategies, and wiring API responses to DB rows.

## Sections
1. Why testers need SQL
2. SELECT / WHERE / ORDER / LIMIT
3. JOIN basics
4. Seed & teardown patterns
5. Test data strategies (fixtures, factories, isolation)
6. API ↔ DB verification
7. Common pitfalls
8. Practice
9. Challenges
10. Answer key

## Labs
- Teaching stand-in: `GET /_db/snapshot` on mock API
- `labs/part3/api-db-check.mjs`
- Study-app SQL playground (in-memory tables)

## Done when
Learner can write a SELECT+JOIN mental model and an API↔DB consistency assert with seed/teardown.
